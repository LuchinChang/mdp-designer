## Summary

## Checklist
- [ ] `pnpm -C web lint && pnpm -C web build` pass (if `web/` changed)
- [ ] `ruff check python` passes (if `python/` changed)
- [ ] If the file format changed: spec, schema, TS types, and Python model updated together
